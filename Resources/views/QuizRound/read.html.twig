<html>
<head>
<style>
body{ font-family: "times new roman", serif; font-size: 12px; }
h2{ padding: 0; margin: 0;margin-top: 1em; }
p{ text-aligh: justify; }
ul{ float: left }
</style>
</head>

<body>
{% block rbody %}
<span style="font-size: 40px;">{{ writer.uniqueIdentifier }}</span>
{% endblock %}

{% block body %}
  <h2>{{ config.name }}</h2>
  <span id="timeStill"></span>
{% for k, task in config.tasks %}
  <div style="clear: both;"></div>
  <div class="feladat" id="feladat{{ k }}">
  <div style="clear: both;"></div>
    <h3 style="">{{ task.name }}</h3>
    <div style="clear: both;"></div>
    {% include 'EotvosVersenyrBundle:QuizRound:task_' ~ task.type ~ '.html.twig' with { 'task': task, 'taskk' : k  } %}
    <div style="clear: both;"></div>
  </div>
  <div style="clear: both;"></div>
{% endfor %}
{% endblock %}

</body>
</html>
